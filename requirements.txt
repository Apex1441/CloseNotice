# Pinned explicitly (not a declared app dependency, but bundled with every
# fresh venv/pip install at whatever version happened to ship that day).
# PYSEC-2026-3447 was fixed in 83.0.0 - this is what pip-audit was flagging.
setuptools>=84.0.0

# HTTP client for APIs
requests>=2.34.2,<3.0.0

# Official Groq SDK
groq>=1.7.0,<2.0.0

# Environment variable management
python-dotenv>=1.2.3,<2.0.0

# CSV operations and data manipulation
pandas>=3.0.5,<4.0.0

# Retry logic with exponential backoff
tenacity>=9.1.4,<10.0.0

# Rate limiting decorators
ratelimit>=2.2.1,<3.0.0

# HTML parsing for scraper
lxml>=6.1.3,<7.0.0
beautifulsoup4>=4.15.0,<5.0.0
html5lib>=1.1,<2.0.0

# Testing
pytest>=9.1.1,<10.0.0
pytest-cov>=7.1.0,<8.0.0
pytest-mock>=3.15.1,<4.0.0
requests-mock>=1.12.1,<2.0.0
